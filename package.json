{
  "name": "dhscycle",
  "version": "1.0.0",
  "description": "Scheduling app for high school",
  "main": "server/dist/index.js",
  "scripts": {
    "build": "cd server && tsc --build --verbose && cd ../client && webpack && cd .."
  },
  "devDependencies": {
    "html-webpack-plugin": "^5.6.0",
    "ts-loader": "^9.5.1",
    "typescript": "^5.5.4",
    "webpack": "^5.93.0",
    "webpack-cli": "^5.1.4"
  }
}
